WCR_DOMAIN=weechatradio.com
# Hub mail (Resend). Never commit the real key — set only in /opt/wcr/.env on the droplet.
# Use the "WeeChat Radio" API key; send only from *@mail.weechatradio.com (not apex MX).
RESEND_API_KEY=
# Optional: postgres (v1 uses SQLite in /data)
# POSTGRES_PASSWORD=change-me
