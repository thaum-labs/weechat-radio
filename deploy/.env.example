WCR_DOMAIN=weechatradio.com
# Optional: postgres (v1 uses SQLite in /data)
# POSTGRES_PASSWORD=change-me
# Hub only. Real values stay in /opt/wcr/.env on the droplet, never in git.
# RESEND_API_KEY=
# RESEND_WEBHOOK_SECRET=
